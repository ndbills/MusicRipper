# Review Workflow

> **Status:** Stub. Full guide lands in **Phase 7** once `_ReviewQueue/`
> exists (Phase 5) and `Move-FromReviewQueue.ps1` is written (Phase 7).

Planned contents:

- How to read `REVIEW.txt`.
- How to drag a review-queue album folder into MusicBrainz Picard for
  re-tagging.
- How to mount `_image/<Album>.flac` for inspection (foobar2000 or
  WinCDEmu / Virtual CloneDrive).
- How to re-rip — just reinsert the disc; the tool will detect the
  existing queue entry and offer to replace it.
- How to promote a fixed-up album into the main library with
  `src/tools/Move-FromReviewQueue.ps1`.
